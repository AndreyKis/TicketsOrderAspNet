@model Common.TransportType

@{
    ViewBag.Title = "Додати новий транспорт";
    Layout = "~/Views/Shared/_Layout.cshtml";
    bool canAdd = ViewBag.canAdd;
}

@if (canAdd)
{
    @:<h2>Введіть новий транспорт</h2>
}

else
{
    @:<h2>Вибачте, але такий транспорт вже існує, спробуйте ще раз.</h2>
}

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Тип транспорту</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Назад до списку", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
