@model Common.Rate

@{
    ViewBag.Title = "Додати нову кількість поїздок";
    Layout = "~/Views/Shared/_Layout.cshtml";
    bool canAdd = ViewBag.canAdd;
}

@if (canAdd)
{
    @:<h2>Введіть значення нової кількості</h2>
}

else
{
    @:<h2>Вибачте, але така кількість вже існує, спробуйте ще раз.</h2>
}

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Кількість поїздок</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Назад до списку", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
